# Copy this file to `.env` on the server (project root) — used by docker compose (backend service).
# NEVER commit the real .env file.

MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/business_experts
JWT_SECRET=generate-a-long-random-string-here
ADMIN_EMAIL=surajkurrey956@gmail.com
ADMIN_PASSWORD=change-me

# Optional: Cloudinary for media uploads (otherwise files are stored on the server)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
