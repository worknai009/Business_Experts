{
  "name": "business-experts-mern",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\" \"npm --workspace admin run dev\"",
    "build": "npm --workspace frontend run build && npm --workspace admin run build",
    "start": "npm --workspace backend start"
  },
  "workspaces": [
    "frontend",
    "backend",
    "admin"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "react-router-dom": "^7.18.1"
  }
}
